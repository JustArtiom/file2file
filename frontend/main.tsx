import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById(`root`)!).render(
  <React.StrictMode>
    <h1 className="text-3xl font-bold underline">Hello world from React!</h1>
  </React.StrictMode>
);